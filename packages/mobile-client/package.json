{
  "name": "mobile-client",
  "version": "25.10.0",
  "license": "MIT",
  "files": [
    "build"
  ],
  "scripts": {
    "build:ios": "cap sync ios",
    "build:android": "cap sync android",
    "build:bundle": "./bin/create-bundle",
    "build": "yarn build:ios && yarn build:android && yarn build:bundle",
    "start:ios": "yarn build:ios && cap run ios",
    "start:android": "yarn build:android && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capgo/capacitor-updater": "7.18.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capgo/cli": "^7.17.6"
  }
}
